pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url = uri("https://jitpack.io") } // ✅ correct syntax for Gradle 8+
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS) // ✅ avoid conflicts
    repositories {
        google()
        mavenCentral()
        maven { url = uri("https://jitpack.io") } // ✅ correct syntax
    }
}

rootProject.name = "WhatsOrder"
include(":app")

// Capacitor Android core
include(":capacitor-android")
project(":capacitor-android").projectDir = file("../node_modules/@capacitor/android/capacitor")

// If you add capacitor plugins, include them too
// Example:
// include(":capacitor-cordova-android-plugins")
// project(":capacitor-cordova-android-plugins").projectDir = file("../node_modules/@capacitor/android/capacitor-cordova-android-plugins")
